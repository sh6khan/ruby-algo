# given an undericted graph, find the center most node[s] in O(n) time

class CenterFinder
  # iterate over every node, looking for nodes with only one connection
  def self.find_center(graph)

    # find the edge nodes
    



  end

end
